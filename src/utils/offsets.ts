// Custom offsets for each section
// Adjust values as needed for your layout/header

export const SECTION_OFFSETS: { [key: string]: number } = {
  about: 20,
  skills: 10,
  projects: 10,
  contact: 10,
  testimonials: 50,
  services: 10,
};

// Optionally, export a default offset
export const DEFAULT_OFFSET = 80;
